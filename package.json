{
  "name": "meeting-bingo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Online multiplayer custom bingo for meetings, on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
