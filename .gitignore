# Dependencies (reinstalled with `npm install`)
node_modules/

# Local dev secrets — never commit (use `wrangler secret put` for prod)
.dev.vars
.dev.vars.*
# …but DO track the template
!.dev.vars.example

# Wrangler local state / build output
.wrangler/
dist/

# OS / editor cruft
.DS_Store
*.log
